import AppProvider from './provider';
// import { AppRouter } from './router';

const App = () => {
  return (
    <AppProvider>
        <div>Temp</div>
      {/* <AppRouter /> */}
    </AppProvider>
  );
};

export default App;
